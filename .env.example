# Supabase environment configuration for Pilot Flight Tracker
# Copy this file to `.env` (or `.env.local` depending on your setup)
# and fill in with values from your Supabase project settings.

# Supabase project URL (e.g. https://xyzcompany.supabase.co)
VITE_SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"

# Supabase public anon key (from Settings → API → anon public)
VITE_SUPABASE_ANON_KEY="YOUR_ANON_PUBLIC_KEY_HERE"
